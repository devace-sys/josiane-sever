-- Migration to add mustChangePassword and inviteToken fields to User model
-- Run this after updating the Prisma schema

-- Note: This is a reference migration. Use `npx prisma migrate dev` to generate the actual migration.

